{
  "name": "scavenger-hunt-monorepo",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "echo \"No tests yet\" && exit 0",
    "build": "echo \"No build yet\" && exit 0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
